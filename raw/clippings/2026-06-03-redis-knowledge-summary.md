---
title: Redis知识点汇总
author: lanFriends
source-url: https://www.cnblogs.com/lanFriends/p/15406252.html
captured-date: 2026-06-03
---

# Redis知识点汇总

支持的数据类型以及应用场景、过期策略、内存淘汰策略、持久化机制、主从、哨兵模式、集群模式、发布订阅、事务、缓存雪崩、缓存穿透、缓存击穿......

## 什么是Redis？

Redis是一个基于C语言开发的key-value形式的NOSQL数据库，它是开源免费的，为了解决高并发、高扩展和大数据存储等一系列的问题而产生的数据库解决方法。

## Redis为什么那么快？

- 基于内存的单线程处理，避免了上下文切换和资源的竞争
- 使用了基于I/O多路复用的网络模型（可了解selector和epoll的区别）
- 单线程是指核心命令(接受命令，解析命令，执行命令，返回结果)的处理是串行
- Redis 4.0后引入了多线程，额外的线程用于后台处理，例如删除对象
- Redis 6.0中，多线程主要用于网络I/O阶段(接受命令和返回结果)

## 支持的数据类型？

支持的基本数据类型有5种：string，hash，list，set，zset

### 1. String

二进制安全的，直接存储整数。内容支持字符串，整数和浮点数。编码有int，embstr和raw三种格式。

**应用场景：**
- 实现缓存：存储常用的固定请求结果
- 计数器：实时计数器，快速计数和查询

### 2. List

有序列表。编码为ziplist或是linkedlist。

**应用场景：**
- 存储列表行的数据结构：粉丝列表，好友列表
- 分页查询：通过lrange读取闭区间的元素
- 阻塞队列：通过lpush插入数据，通过Brpop消费数据

### 3. Set

无序列表，自动去重。

**应用场景：**
- 可以查看共同好友：通过取交集

### 4. Hash

一个键对应多个key-value。

### 5. Zset

有序集合，按score排序。

**应用场景：**
- 热搜排行榜

## Redis中的过期策略

- **定时删除**：为设置过期时间的key添加定时器
- **定期删除**：每隔一段时间，随机检查一部分键的过期时间
- **惰性删除**：在取键的时候才会对键进行过期检查

Redis使用的是**惰性删除**和**定期删除**相结合的策略。

## 内存淘汰策略

| 策略 | 说明 |
|------|------|
| noeviction | 默认策略，不淘汰任何key，直接返回错误 |
| allkeys-lru | 在所有的key中，使用LRU算法淘汰部分key |
| allkeys-lfu | 在所有的key中，使用LFU算法淘汰部分key |
| allkeys-random | 在所有的key中淘汰部分key |
| volatile-lru | 在设置了过期时间的key中，使用LRU算法淘汰部分key |
| volatile-lfu | 在设置了过期时间的key中，使用LFU算法淘汰部分key |
| volatile-random | 在设置了过期时间的key中，随机淘汰部分key |
| volatile-ttl | 在设置了过期时间的key中，挑选ttl短的key淘汰 |

## Redis的持久化机制

### RDB

通过命令BGSAVE和SAVE触发，生成RDB文件。

### AOF

保存Redis服务器执行的写命令来记录数据库的状态。

### 混合持久化

AOF重写时，将重写这一刻的内存做RDB快照处理，然后将RDB快照内容和增量的AOF修改内存数据的命令在一起，都写入新的AOF文件。

## 主从架构

- **完整重同步**：处理初次复制情况
- **部分重同步**：处理断线后重复制情况

## 哨兵模式

保证Redis高可用，当主服务器挂了，通过sentinel能够自动切换到从服务器。

## 集群模式

Cluster模式实现了Redis的分布式存储，每台节点存储不同的内容，解决在线扩容的问题。

## 发布订阅

服务器维护着一个关于频道和订阅者的字典。

## 事务

将多个命令请求打包，一次性按顺序执行。

## 缓存雪崩、缓存击穿、缓存穿透

### 缓存雪崩

大量热点key在同一时间失效，造成数据库瞬时请求量大，压力骤增。

**解决方法：**
- 给key设置随机的失效时间

### 缓存击穿

某个热点key失效的时候，有大量的请求打进来，由于缓存失效，会直接请求数据库。

---

**相关概念：**
- [[Redis 数据类型]]
- [[Redis 过期策略]]
- [[Redis 内存淘汰策略]]
- [[Redis 持久化机制]]
- [[Redis 主从复制]]
- [[Redis 哨兵模式]]
- [[缓存雪崩]]
- [[缓存击穿]]
- [[缓存穿透]]

**相关实体：**
- [[Redis]]
